import React from 'react';
import PropTypes from 'prop-types';

NavigationAccount.propTypes = {
    
};

function NavigationAccount(props) {
    return (
        <div>
            UserDrop
        </div>
    );
}

export default NavigationAccount;